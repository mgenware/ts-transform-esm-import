{
  "name": "example",
  "version": "1.0.0",
  "type": "module",
  "exports": "./dist/main.js",
  "license": "MIT",
  "devDependencies": {
    "del-cli": "^3.0.1",
    "ttypescript": "^1.5.12",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "clean": "del ./dist",
    "dev": "yarn clean && ttsc -w",
    "build": "yarn clean && ttsc"
  },
  "dependencies": {
    "bowhead-js": "^6.0.0"
  }
}
